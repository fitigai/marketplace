import styled from 'styled-components'

export const Container = styled.div`
  width: calc(100%);
  max-width: 1440px;
  padding: 0 20px;
  margin: 0 auto;
`

export const Main = styled.main`
  flex-grow: 1;
`
